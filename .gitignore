Nothing needs to be added to .gitignore since the only modified file is a JSON data file (`predicciones/data/ratings_wc2026.json`) which appears to be source/config content rather than build artifacts, dependencies, or temporary files.